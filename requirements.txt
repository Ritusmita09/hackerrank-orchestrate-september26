# Runtime
fastapi
uvicorn[standard]
sqlalchemy>=2.0
alembic
pydantic>=2
python-dotenv
psycopg[binary]        # PostgreSQL driver (production target)
httpx                   # HTTP client: LLM provider adapter (Phase 4)

# Testing
pytest
